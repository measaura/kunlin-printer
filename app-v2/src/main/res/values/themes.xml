<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Printer" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="textAllCaps">false</item>
        <item name="android:windowBackground">@color/background</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_200</item>
        <item name="colorPrimaryVariant">@color/dark_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/dark_200</item>
        <item name="colorSecondaryVariant">@color/dark_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.Printer.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.Printer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="Theme.Printer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    <style name="BaseDialog" parent="Theme.AppCompat.Dialog">
<!--        <item name="android:windowBackground">@android:color/transparent</item>-->
        <item name="android:windowIsFloating">false</item>
    </style>
</resources>